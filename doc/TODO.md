## 项目 TODO 总览（渐进式）

> 约定：  
> - 状态取值：`pending`（未开始）、`in_progress`（进行中）、`blocked`（阻塞）、`done`（已完成）  
> - 优先级：`P0`（最高）、`P1`、`P2`

### 1. 当前迭代（优先处理）

| 优先级 | 任务 | 状态 | 说明 |
| ------ | ---- | ---- | ---- |
| P0 | 精简首屏加载链路（音频懒加载、非关键模块延后） | in_progress | 减少首页 bundle 体积和初始化时间 |
| P0 | 优化 `/admin` 资源列表首屏加载体验 | pending | 确保在数据量增大时仍然流畅 |
| P1 | 调整 `ProtectedRoute` 鉴权逻辑，只在挂载时拉取会话 | pending | 降低不必要的 Supabase 调用频率 |
| P1 | 为首页缓存（`cachedData`）增加简单容量上限 | pending | 避免长时间使用导致内存占用过大 |

### 2. 下一步迭代（体验与稳定性）

| 优先级 | 任务 | 状态 | 说明 |
| ------ | ---- | ---- | ---- |
| P1 | 为盲盒查询增加结果上限或后端随机接口 | pending | 在资源数上千时仍保持盲盒响应快 |
| P1 | 拆分 `ResourcePreview` 卡片为子组件并 `memo` 优化 | pending | 为将来更多卡片数量预留性能空间 |
| P2 | 简化首页和后台的 Loading 动画效果 | pending | 在低端设备上减少 GPU/动画开销 |

### 3. 中长期优化方向

| 优先级 | 任务 | 状态 | 说明 |
| ------ | ---- | ---- | ---- |
| P1 | 引入性能监控（如 Web Vitals、前端日志上报） | pending | 能够量化优化效果并持续跟踪 |
| P2 | 根据路由拆分更多粒度的代码块（Vite 动态导入） | pending | 让管理后台等非首页路由按需加载 |
| P2 | 为高频接口增加后端层面的缓存 / 索引优化 | pending | 减少数据库负载、提升查询速度 |

### 4. 已完成记录（可持续追加）

> 将已完成的任务移动到此处，方便回顾历史。

| 完成时间 | 任务 | 说明 |
| -------- | ---- | ---- |
| - | - | 目前暂无（完成后请把对应任务从上方表格移动到这里，并将状态改为 `done`） |

